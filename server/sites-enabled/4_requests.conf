server {
  listen   80;
  server_name requests.testing.postmodern.technology requests.hack.postmodern.technology;
  index index.php index.html index.htm;
  root /sites/;
  set $upstream requests;
  proxy_redirect off;
  include blog.conf;
}
